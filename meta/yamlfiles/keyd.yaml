- defaults:
    link:
      relink: true
      create: true
      force: true
    shell:
      stdin: true
      stdout: true
      stderr: true

- link:
    /etc/keyd/default.conf: config/keyd-default.conf

- shell:
    - [sudo keyd reload, Reloading Keyd]
