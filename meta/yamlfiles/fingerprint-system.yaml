- defaults:
    link:
      relink: true
      create: true
    shell:
      stdin: true
      stdout: true
      stderr: true

- link:
    /etc/modprobe.d/bluetooth-blacklist: config/bluetooth-blacklist.conf
