- defaults:
    link:
      relink: true
      create: true
      force: true
    shell:
      stdin: true
      stdout: true
      stderr: true

- link:
    ~/.config/way-displays/cfg.yaml: config/waydisplays-cfg.yaml
# - shell:
#     - [makoctl reload, Reloading mako]
