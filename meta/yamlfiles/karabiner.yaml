- defaults:
    link:
      relink: true
      create: true
    shell:
      stdin: true
      stdout: true
      stderr: true

- link:
    ~/.config/karabiner/karabiner.json: config/karabiner.json

- shell:
    - [brew install --cask karabiner-elements, Installing karabiner-elements]
# - plugins:
#     - meta/dotbot-brew/

# - cask:
#     - karabiner-elements
