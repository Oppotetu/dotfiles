- defaults:
    link:
      relink: true
      create: true
      force: true
    shell:
      stdin: true
      stdout: true
      stderr: true

- link:
    /etc/sddm.conf.d/sddm.conf: config/sddm.conf
# - shell:
#     - command: |
#         if ! sudo test -f /etc/sudoers.d/env_keep_xdg; then
#           echo 'Defaults env_keep += "XDG_CONFIG_HOME"' | sudo EDITOR='tee -a' visudo -f /etc/sudoers.d/env_keep_xdg
#         fi
#       description: Ensure XDG_CONFIG_HOME preserved in sudo
